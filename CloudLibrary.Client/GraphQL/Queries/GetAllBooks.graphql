query GetAllBooks {
  books {
    items {
      ...BookItem,
      authors {
        items {
          id,
          first_name,
          middle_name,
          last_name
        }
      }
    }
  }
}

fragment BookItem on Book {
    id,
    title,
    year,
    pages
}