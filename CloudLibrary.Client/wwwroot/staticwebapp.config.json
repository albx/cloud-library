{
  "routes": [
    {
      "route": "/login",
      "allowedRoles": [ "anonymous" ],
      "rewrite": "/.auth/login/github"
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": [ "/images/*.{png,jpg,gif}", "/css/*" ]
  }
}